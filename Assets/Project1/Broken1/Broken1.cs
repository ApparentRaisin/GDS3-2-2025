using UnityEngine;

//This was meant to be my first person character controller
//its not working but I dont know why?
//I have a bunch or red errors, but even before those it didnt work right
//the rotations where all weird

//Everything between /* and */ is broken

/*
public class Broken1 : MonoBehaviour
{
    
    public Transform player;
    
    void Start()
    {
        .//player = this.transform;
    }

    
    void update()
    {
        player.rotation *= Quaternion.Euler(Input.GetAxis("MouseY"), Input.GetAxis("MouseX"), 0) *

        player.position += new Vector3(Input.GetAxis("Horizontal"), 0, Input.GetAxis("Vertical")) * Time.deltaTime * 5;

        
    }
}
*/